# TODO (lexora-website)

- [ ] Update About page “What we do” icons to use `lucide-react` outline variants
- [x] Modify `src/components/about/CapabilityCard.js` to import and render an outline icon instead of the bullet

- [x] Pass an `Icon` key from `src/components/about/CapabilityGrid.js` down to `CapabilityCard`

- [x] Map each capability pillar to a reasonable Lucide outline icon (e.g., `Scale`, `ShieldCheck`, `Handshake`, `SearchCheck`, `Building2`)

  - [ ] Verify Tailwind classes + icon sizing/colors match existing design
  - [ ] Run `npm run lint` and `npm run build` to ensure no errors

